openapi: 3.0.3
info:
  title: API de ThesisFlow
  description: Documentación colaborativa del equipo
  version: 1.0.0
paths:
  /ping:
    get:
      summary: Endpoint de prueba
      responses:
        "200":
          description: OK
